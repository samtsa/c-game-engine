{
    "version": "2.0.0",
    "tasks": [
        {
            "type": "shell",
            "label": "C/C++: gcc build active file",
            "command": "/usr/bin/g++",
            "args": [
                "-g",
                "${workspaceFolder}//Tester.cpp",
                "-o",
                "${workspaceFolder}//main.exe"
            ],
            "options": {
                "cwd": "${fileDirname}"
            }
        }
    ]
}